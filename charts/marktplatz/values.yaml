# Default values for marktplatz.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# deploymentMode: "monolith" or "microservices"
deploymentMode: microservices

replicaCount: 1

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# Configuration for both modes
config:
  logLevel: info
  database:
    # If using the internal postgresql (postgresql.enabled=true), the server should be:
    # <release-name>-marktplatz-postgresql
    server: marktplatz-postgresql
    port: 5432
    name: app_store
    user: postgres
    password: "changeme"
  jwtSecret: "changeme-secret"
  oidc:
    enabled: true
    issuer: "https://keycloak.example.com/realms/myrealm"
    clientId: "marktplatz"
    clientSecret: "changeme"
    adminGroup: "admin"
    insecure: true
  cors:
    allowedOrigins: ["*"]

# Monolith specific configuration (both frontend and backend in one container)
monolith:
  image:
    repository: gartifactory-jfrog.apps.ocp4.svc.prod.pl2cloud.de/custom-shared-images/jneubert/plain-marktplatz
    pullPolicy: IfNotPresent
    tag: "latest"
  service:
    type: ClusterIP
    port: 80
    backendPort: 8082
    frontendPort: 3000
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi

# Microservices specific configuration
backend:
  replicaCount: 1
  image:
    repository: gartifactory-jfrog.apps.ocp4.svc.prod.pl2cloud.de/custom-shared-images/jneubert/plain-marktplatz-backend
    pullPolicy: IfNotPresent
    tag: "latest"
  service:
    type: ClusterIP
    port: 8182 # External port for the service
  resources:
    limits:
      cpu: 300m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

frontend:
  replicaCount: 1
  image:
    repository: artifactory-jfrog.apps.ocp4.svc.prod.pl2cloud.de/custom-shared-images/jneubert/plain-marktplatz-frontend
    pullPolicy: IfNotPresent
    tag: "latest"
  env:
    # URL of the backend API (internal or external)
    # Default assumes microservices mode with backend service name
    apiUrl: "http://marktplatz-backend:8182"
    nextauthUrl: "http://marktplatz.example.com"
    authSecret: "secret-for-nextauth"
  service:
    type: ClusterIP
    port: 80
  resources:
    limits:
      cpu: 300m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

postgresql:
  enabled: true
  image: 'postgres:16'
  pullPolicy: IfNotPresent
  persistence:
    enabled: true
    storageClassName: default
    size: 20Gi
  auth:
    database: "marktplatz"
    password: "marktplatz"
    username: "marktplatz"
  service:
    type: ClusterIP
    port: 5432
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

nodeSelector: {}

tolerations: []

affinity: {}
