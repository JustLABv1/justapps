{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "marktplatz.fullname" . }}-postgresql-config
  labels:
    {{- include "marktplatz.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
data:
  POSTGRES_DB: {{ .Values.postgresql.auth.database | default "postgres" }}
  POSTGRES_USER: {{ .Values.postgresql.auth.username | default "postgres" }}
{{- end }}